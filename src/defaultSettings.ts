import { ISettings } from "./AppSettings";

const defaultSettings: ISettings = {
  tests: {
    periodicTable: {
      elements: null
    },
    valences: {
      elements: null
    }
  },
  theme: "light"
};

export default defaultSettings;
